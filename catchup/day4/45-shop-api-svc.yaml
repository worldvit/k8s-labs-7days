apiVersion: v1
kind: Service
metadata:
  name: shop-api
  namespace: shop
  labels:
    app: shop
    tier: api
spec:
  type: ClusterIP
  selector:
    app: shop
    tier: api
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP
